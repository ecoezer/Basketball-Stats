rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents (for the stats site)
    match /{document=**} {
      allow read: if true;
      // Deny write access to everyone (except Admin SDK which bypasses rules)
      allow write: if false;
    }
  }
}
